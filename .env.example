# Server settings (all optional)
PORT=8787
# Admin sign-in: email + password, plus a personal link (/admin?token=...)
ADMIN_EMAIL=admin@apollo.st
ADMIN_PASSWORD=apollo
ADMIN_LINK_TOKEN=admin-apollo-crossword
# DATA_DIR=./data
# SEED_DEMO=false
